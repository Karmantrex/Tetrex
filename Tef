openssl x509 -in local-cert.pem -noout -subject -issuer -text | grep -A1 "Subject Alternative Name"

# hashes must match:
openssl x509 -noout -modulus -in local-cert.pem | shasum
openssl rsa  -noout -modulus -in local-key.pem  | shasum
