  unhealthy := to_number(raw_unhealthy)           else unhealthy := 
  interval  := to_number(raw_interval)            else interval  := 
  timeout   := to_number(raw_timeout)             else timeout   := 
