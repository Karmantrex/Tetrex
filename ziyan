I'm trying to filter multiple ASGs using a multi-value dropdown in my dashboard. My $Enter_ASG_name parameter is set as an array (e.g., ["asg-1", "asg-2", "asg-3"]), and I'm using the following query:


Issue:

When I select multiple ASGs in the dropdown, the query returns "Not Found" instead of showing results for all selected ASGs.
If I select only one ASG, it works fine.
If I leave it empty, it correctly shows all ASGs.
What I need:

Ensure that multiple ASGs can be selected in the dropdown and the query correctly returns data for all selected ASGs.
If no ASG is selected, it should return results for all ASGs.
