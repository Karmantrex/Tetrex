#!/bin/bash

if [ "$#" -eq 0 ]; then
  echo "Usage: $0 <path1> <path2> ... <pathN>"
  exit 1
fi

for path in "$@"; do
  echo "Deleting: $path"
  sudo rm -rf "$path"
done

echo "Done. All specified FocusMe files (if found) should now be gone."
