#!/bin/bash

# Read the input file line-by-line and store it as an array
mapfile -t items < input.txt

# Join the array into a quoted, comma-separated string
quoted_list=$(printf '"%s",' "${items[@]}")
quoted_list=${quoted_list%,}  # Remove the trailing comma

# Output the result
echo "$quoted_list"
