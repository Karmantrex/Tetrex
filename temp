fetchurl() {
  local url=$1
  local sha=$2
  local file="${DIRENV_TMP_DIR}/$(basename "$url")"
  if [ ! -f "$file" ]; then
    echo "Fetching $url..."
    curl -L -o "$file" "$url"
  fi
  echo "$file"
}
