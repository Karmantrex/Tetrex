#!/bin/bash

# Initialize an empty result string
result=""

# Read each line from input.txt and append to the result
while IFS= read -r line || [[ -n "$line" ]]; do
    result+="\"$line\","
done < input.txt

# Remove the trailing comma and echo
echo "${result%,}"
